/*

    # COMPONENTS

    apps are build of components

    app starts with the app root component (which holds all the other components)

    each component has its own business logic (app is thus split
    in re-usable small components)
*/