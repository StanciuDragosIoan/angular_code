/*
    # CREATING  A COMPONENT

    all other components beside app-root will be added in
    the app-root (their selectors will be placed in there)

    created a 'server' folder in the app folder (in 'src')
        created a server.component.ts file inside the 'sever' folder
        created an server.component.html inside the 'server' folder

            imported component from angular core at the top
            added the @Component decorator and passed in object with selector and template:
                @Component({
                    selector: 'app-server',
                    templateUrl: './server.component.html'
                })

            created the component class



*/