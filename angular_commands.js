/*
    ng new app-name     (generates new app)

    ng serve --open (serves app on dev server in browser and opens window)

    npm i bootstra --save (installs locally in this project bootstrap dependency)

    ng generate component/g c + name (generates component and adds it to the
    app.modules.ts both the import and the declaration)

*/